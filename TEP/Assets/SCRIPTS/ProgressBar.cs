using UnityEngine;
using UnityEngine.UI;

public class ProgressBar : MonoBehaviour
{
    // necesito que mientras avanza el personaje vaya aumentando o deteniendose dependiendo la distancia del personaje
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
